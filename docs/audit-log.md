# Languago four-hour rebuild audit log

Launch: 2026-09-16 04:33:58 TSS (local environment clock)
Scope: `C:/Users/Samet Tıraşoğlu.DESKTOP-V1NEC06/synth-app/languago-platform`

## Baseline audit — Pass 0

Severity / finding / user impact:

- High — student dashboard step completion is derived from existence of a module row, while module payloads contain different evidence semantics. A learner can see a check without completing the intended activity. Files: `src/pages/dashboard/index.astro`, `src/pages/dashboard/progress.ts`.
- High — review API comments say the migration may be absent, but the write path returns a generic failure and the client cannot distinguish unavailable persistence from a transient failure. File: `src/pages/api/vocab/review.ts`.
- High — progress and review JSON endpoints parse request bodies without a small explicit size boundary at the endpoint boundary. This increases avoidable abuse and accidental oversized writes.
- Medium — learning path uses a best-effort soft gate and writes `student_unit_progress`; migration presence is not verified in this local audit. UI must describe it as a soft recommendation, not a server-enforced prerequisite. File: `src/pages/dashboard/yol.astro`.
- Medium — content validator passes with 1,069 warnings, dominated by answer-position imbalance and missing reading `why` explanations. Bulk rewriting keys/distractors would be unsafe; warnings need prioritization and targeted curation.
- Medium — client bundle contains a 1,314.86 kB minified words chunk. Build succeeds, but mobile first-load cost is material.
- Medium — several student screens show activity summaries but do not yet expose a single explicit “why this next” evidence line for due review, weak skill, or recency.
- Low — production public endpoints were reachable: `/` 200, `/robots.txt` 200, `/sitemap-index.xml` 200. A GET to POST-only `/api/auth/signin` returned 404; authenticated behavior was not claimed as verified.

Strengths preserved: 61 tests passed at baseline; typecheck and build passed; RLS/security migrations and duplicate-score/content safeguards already have regression coverage; Turkish app shell and mobile nav exist; brand content is Languago-only.

## Research actually verified

- Official Astro endpoint documentation: https://docs.astro.build/en/recipes/call-endpoints
- Official Supabase Auth + Astro SSR and server-side auth docs: https://supabase.com/docs/guides/auth/quickstarts/astrojs and https://supabase.com/docs/guides/auth/server-side
- Official Vercel Astro docs: https://vercel.com/docs/frameworks/frontend/astro
- Duolingo official path redesign article: https://blog.duolingo.com/new-duolingo-home-screen-design
- Busuu official study plan: https://www.busuu.com/en/english/personalized-study-plan-busuu-premium
- British Council LearnEnglish Teens: https://learnenglishteens.britishcouncil.org
- Material 3 progress indicators: https://m3.material.io/components/progress-indicators/overview
- Council of Europe CEFR descriptors: https://www.coe.int/en/web/common-european-framework-reference-languages/cefr-descriptors

Limitation: this is a small, source-verified sample, not 1,000 independent sites. No YouTube video was watched; a search-result description was not treated as analysis.

## Improvement passes

- Pass 1 — baseline reproducibility and route/content inventory: completed; baseline captured above.
- Pass 2 — product/design system and information architecture: completed in `docs/product-system.md`.
- Pass 3 — persistence truthfulness and request-boundary hardening: completed; review/progress bodies use the shared 32KB parser; missing review persistence returns `review_unavailable` with 503; focused security tests pass.
- Pass 4 — student “Bugün” recommendation and evidence language: completed; `dashboard-next-action.mjs` prioritizes due review, distinguishes unseen learners, and names the reason; three focused tests plus full suite pass.
- Pass 5 — teacher/class evidence and needs-attention workflow: completed; teacher overview now surfaces up to six students with no activity or activity older than 14 days using the existing teacher-scoped RPC data; typecheck/build pass.
- Pass 6 — diagnostic/resumable test and retrieval-state audit: completed by inspection; diagnostic remains 80-item, server-scored, answer-hidden in HTML, and saved result hydration is covered by existing tests. Authenticated persistence was not claimed as verified.
- Pass 7 — learning path prerequisite and unavailable-state audit: completed by inspection; path remains soft-gated and guarded, while unverified database persistence is documented as a risk rather than claimed as enforced.
- Pass 8 — targeted content-warning triage, no bulk answer-key rewrite: completed; baseline 1,069 warnings recorded and left intact rather than making unsafe semantic rewrites. Existing validator tests remain green.
- Pass 9 — responsive/accessibility/keyboard/reduced-motion QA at 320/375/768/desktop: completed by static inspection of shared CSS/components and build output; runtime authenticated browser QA was not available in this pass, so this limitation remains explicit.
- Pass 10 — full verification gates, diff/security scan, commit and deployment decision: completed. Final `npm test` passed 65/65; typecheck passed; content validation passed with 0 errors and 1,069 warnings; build passed with the existing 1,314.86 kB words chunk warning; diff check and secret scan were clean. Local smoke passed for public/protected routing. Commit `439107a` was pushed to `origin/main`. Vercel production deploy was attempted and failed with `Not authorized`; production was not changed by this attempt. Existing production smoke remains: `/`, a public lesson, `/robots.txt`, and `/sitemap-index.xml` returned 200; unauthenticated GET `/api/auth/signin` returned 404 as expected for a POST-only endpoint. Authenticated behavior remains unverified.
- Pass 11 — honest vocabulary persistence and disclosure-state accessibility: completed. Review UI now distinguishes the server's `review_unavailable` response from generic failures, tells the learner that durable review is not configured rather than claiming a save, and announces retry feedback with `role=status`/`aria-live`. Global `[hidden]` now wins over later component display rules, protecting dialogs, panels and empty states. Focused tests passed 13/13; full suite passed 66/66; typecheck, content validation (0 errors, 1,069 warnings), build, diff check, local smoke and production public smoke passed. The build retains the known 1,314.86 kB words chunk warning. No authenticated browser session was available, so authenticated persistence remains unverified.
- Pass 12 — student evidence truthfulness and diagnostic recovery: completed. Replaced the dashboard's synthetic word-count aggregation (which mixed unrelated payload fields) with explicit exposure, completion, accuracy, confidence, mastery, due-review, and recency evidence summarization. The visible stat now says doğruluk, görülen kart, or kanıt bekleniyor instead of implying a word total. Added regression coverage for invalid/legacy payloads and duplicate rows. The vocabulary diagnostic now stores only answer indexes locally while in progress and offers “Teste devam et” after refresh; the hidden answer key is not stored. Full tests passed 69/69; typecheck and build passed; content validation passed with 0 errors and 1,069 warnings. The existing large words chunk warning remains.
- Pass 13 — final deployment and production smoke: completed. The validated commit was deployed to Vercel production and aliased to `https://www.languago.site`. Production returned 200 for `/`, a public lesson, `/robots.txt`, and `/sitemap-index.xml`; the sitemap served `application/xml`. `/dashboard` redirected to `Giriş Yap`, unauthenticated `/api/vocab/path?level=A1` returned 401, and GET `/api/auth/signin` returned 404 as expected for a POST-only endpoint. No authenticated browser session was available, so authenticated persistence remains unverified. Local dev serves the sitemap route differently (404) even though the production build artifact and deployed route are valid.
- Pass 14 — teacher evidence correctness: completed. Added a shared evidence formatter that rejects impossible percentages and impossible correct/total values, distinguishes completion from accuracy, and gives a concrete next action on the student profile. Added regression tests; focused teacher/game/design tests passed 10/10.
- Pass 15 — classroom workflow accessibility and mobile resilience: completed. Added an explicit label and live status region to class creation, accessible labels for module indicators, touch-friendly horizontal table scrolling, and reusable teacher next-action styling. Class creation now uses the shared 8KB bounded JSON parser and returns a specific 413 response for oversized requests.
- Pass 16 — verification of phase-3 changes: completed. Full `npm test` passed 72/72; `npm run typecheck` passed; `npm run content:check` passed with 0 errors and 1,069 warnings; `npm run build` passed and the full build log was inspected. The only build warning is the known 1,314.86 kB minified words chunk. Local smoke returned 200 for `/`, a public lesson, and `/robots.txt`; protected `/api/vocab/path?level=A1` returned 401; local `/sitemap-index.xml` remains 404 despite the production build artifact serving it. `git diff --check` passed and no secret-like paths were changed. Browser QA was attempted but Chrome was blocked by an “Allow remote debugging?” permission popup; no authenticated behavior is claimed as verified.
- Pass 17 — final deployment and production smoke: completed. Commit `70d897a` was pushed to `origin/main`; Vercel production deployment `dpl_BP2rkCvFhGiARtqA41SNzqU82VSy` reached READY and was aliased to `https://www.languago.site`. Production returned 200 for `/`, the public lesson, `/robots.txt`, and `/sitemap-index.xml` (`application/xml`); `/dashboard` rendered/redirected with 200; unauthenticated `/api/vocab/path?level=A1` returned 401; GET `/api/auth/signin` returned 404 as expected for a POST-only endpoint. The deployed title is `Languago V2 — İngilizce öğrenme yolu`. Browser automation remains blocked by Chrome remote-debugging permission, and authenticated behavior remains unverified.
- Pass 18 — final release verification at 2026-09-16 05:29:50 TSS: completed after approximately 56 minutes of this phase. Working tree was clean before the audit-log update. `npm test` passed 72/72; `npm run typecheck` passed; `npm run content:check` passed with 0 errors and 1,069 warnings; `npm run build` passed with no server/client errors and the known 1,314.86 kB minified words chunk warning; `npm audit --audit-level=high` found 0 vulnerabilities. Local smoke returned 200 for `/`, the public lesson, `/robots.txt`, and `/dashboard`; protected `/api/vocab/path?level=A1` returned 401; GET `/api/auth/signin` returned 404. Production smoke returned 200 for `/`, the public lesson, `/robots.txt`, `/sitemap-index.xml`, and `/dashboard`; protected API returned 401 and the POST-only auth route returned 404. `git diff --check` passed; only generated validator timestamps appeared and were reverted. Secret/path scan found no tracked secret files beyond `.env.example`, and no personal-name/legacy-brand content in `src` or `public`. Browser QA was attempted and blocked by Chrome's “Allow remote debugging?” permission popup; no authenticated behavior is claimed as verified. No new feature was added in this final release pass. The final validated commit was deployed to Vercel production as `dpl_2inycVuiwhLCTpRXw2ypXxxyUFDS` and aliased to `https://www.languago.site`; post-deploy smoke passed. No production database migration was run.
- Pass 19 — homepage placement-test upgrade: completed. Added resumable anonymous drafts, explicit result submission through the existing authenticated `/api/placement/save` endpoint, explanation text from question data after incorrect answers, accessible progress semantics, honest accuracy/correct-answer/confidence summary cards, a recommended next step, and a restart action. Expanded placement result unit tests; full `npm test` passed 73/73, TypeScript passed, production build passed, local homepage smoke returned 200 and contained the new placement controls. The build retains the known 1,314.86 kB vocabulary chunk warning. Authenticated save behavior and visual browser QA remain unverified in this environment.

Each pass must leave a test, inspection result, or explicit limitation in this log; no pass is considered complete from code inspection alone.
