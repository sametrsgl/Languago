# Classroom served-bank correction — 20260921-languago-loop-1401

## Scope and priority
One bounded implementation run. Baseline main/origin main: `773ea1f19e68d4529a1cc10ff8c0c0fef3bbd0a0`. Existing generated content audit/registry diffs were excluded. Previous Escape, round-reset, explanation and modal fixes already passed live desktop/mobile tests; none were reimplemented. No schedule changes.

## Finding (High / Content)
URL: https://www.languago.site/sinif-oyunu

The production route independently converted gap-fill answers into MCQ distractors, bypassing the already-tested `buildCuratedClassTopics`. Live JSON contained 97 topics, 396 synthetic practice items, and 66 items with fewer than four options. Some generated alternatives were defensible: `I wish it ___ raining.` offered both `were` and keyed `would stop`, while claiming `were` was unsuitable. That is misleading feedback, not a deliberate distractor to preserve.

Reproduce: parse `#cm-topics`, inspect tier-2 questions and count option arrays shorter than four. Run `CLASSROOM_SMOKE_BASE=https://www.languago.site node scripts/smoke-classroom-bank.mjs` against the baseline deployment: exit 1. The regression compares the actual served bank against curated source presentation, not source-text regex.

## Root cause and change
The page had its own unchecked generator; unit tests exercised a separate unused curated helper. Wire the page to the existing helper and remove the redundant generator. No new dependency, source question edits, scoring changes or new mechanic. All 97 topics remain, with 1,982 curated questions and at least 20 distinct stems per topic. The board remains 24 tiles with 19 non-repeating question tiles. Existing answer and explanation indices move together through the helper's stable option presentation.

Files: `src/pages/sinif-oyunu.astro`, `scripts/smoke-classroom-bank.mjs`, `DECISIONS.md`, this report, and G01 roadmap verification note.

## Verification
- New served-bank browser regression: baseline production fails; corrected local route passes at 1440×950 and 390×650.
- Every served topic/item compared against the curated helper; all topic counts, minimum pool size, stem uniqueness, option counts and answer indices checked.
- Full boards completed on desktop/mobile: 19 distinct questions, displayed answers/explanations match served data, zero console/page errors.
- Existing focus, restart, dismissal and level-selection browser regressions all pass on desktop/mobile.
- `npm test`: 174 passed, 0 failed.
- `npm run typecheck`: exit 0.
- `npm run build`: exit 0, client and server completed. Full log inspected; no Unexpected/Build failed/error TS/is not defined. Existing large words chunk warning remains.
- Content validator before/after: 0 errors, 1,068 warnings (73 answer-distribution, 990 explanation, 5 reused reading-stem warnings).
- Active registered questions: 3,167; independently counted active unique IDs: 3,167. Historical registry retains 3,177 entries and 3,177 unique IDs, no collisions. No registry rewrite committed.
- `git diff --check`: pass.
- Security scope review: no auth, RLS, credentials, persistence or API changes; JSON `<` escaping and DOM text escaping retained. No learner data accessed. This is not a full security audit.

Local evidence: `C:/hermes/workspace/languago-1401-{tests,types,build,content-before,content-after,reproduction}.log`; screenshots `C:/hermes/workspace/languago-1401-evidence/curated-1440.png` and `curated-390.png`.

## Limits and next milestone
Curated means authored MCQ source, not expert approval of every item. The existing 1,068 warnings remain QA debt. No authenticated learner flow or screen-reader testing performed. Prioritize explanation/ambiguity sampling before the next G02 A2 information-gap implementation. Production commit, deployment read-back and final live smoke results are recorded in the operations-hub task report after release.
